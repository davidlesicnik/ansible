---
- name: Run full upgrade via apt
  hosts: "{{ target_hosts | default('all') }}"
  become: true
  
  tasks:
    - name: Upgrade the OS (apt dist-upgrade)
      ansible.builtin.apt:
        update_cache: yes
        upgrade: dist